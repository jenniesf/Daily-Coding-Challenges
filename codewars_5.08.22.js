        // CRASH OVERRIDE
function aliasGen( x, y){

        let firstLetter = x[0].toUpperCase()
        let secondLetter = y[0].toUpperCase()

        return  !firstName[firstLetter] ||  !surname[secondLetter] ? "Your name must start with a letter from A - Z." : firstName[firstLetter] + ' ' +surname[secondLetter]
        
}